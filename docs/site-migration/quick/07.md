# 统一返回结构

适用：Suxin Framework V1.0.0 / Suxin CLI V1.0.0。

规范路由组可调用 group.Middleware(ghttp.MiddlewareHandlerResponse)。处理器返回 Res 与 error，中间件生成统一 JSON 结构。成功、参数错误和服务错误都应有可识别的状态。

业务后台使用 utility/gf 中的响应封装及现有加密协议，前端请求封装会解包；不要把基础示例的默认响应中间件直接叠加到后台路由，否则可能重复包装。接入新接口先查看相邻控制器如何返回响应。
