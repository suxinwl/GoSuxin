# 请求数据结构

适用：Suxin Framework V1.0.0 / Suxin CLI V1.0.0。

把零散参数集中到请求类型：
```go
type GreetReq struct {
 Name string `json:"name" v:"required|length:1,40"`
}
```
结构体字段必须导出。json 标签决定外部字段名，v 标签表达校验。传统处理器可以使用 r.Parse(&req)，务必处理返回错误；规范路由可自动解析与校验。错误响应应让调用方识别失败，不能在解析失败后继续写数据库。
