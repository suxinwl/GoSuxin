# 接口文档

适用：Suxin Framework V1.0.0 / Suxin CLI V1.0.0。

请求的 g.Meta 与字段标签构成 OpenAPI 描述。独立示例可配置 s.SetOpenApiPath("/api.json") 和 s.SetSwaggerPath("/swagger")；启动后检查 /api.json 包含自己的路径、参数和响应。

补充 summary、tags、字段说明及验证规则，让描述与实际行为一致。文档页面不替代鉴权。对外部署前检查文档中是否暴露内部管理路由，按部署需求限制访问。
