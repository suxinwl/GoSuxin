# 中间件

适用：Suxin Framework V1.0.0 / Suxin CLI V1.0.0。

在 s.Group("/", func(group *ghttp.RouterGroup) { ... }) 中先注册中间件，再 Bind 控制器。中间件执行 r.Middleware.Next() 进入后续链，返回后可以记录耗时、读取错误或处理结果。

鉴权失败时应返回并终止后续处理。公开示例不需要登录，但业务后台必须保留登录和 RBAC 中间件。日志应记录请求标识、耗时和结果，避免记录密码、令牌及云存储密钥。
