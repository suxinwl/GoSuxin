# 下一步

适用：Suxin Framework V1.0.0 / Suxin CLI V1.0.0。

进入完整业务项目后，依次阅读项目结构、后端流程、权限、数据库、文件管理和部署排错。先开发一个独立的小模块，完成请求定义、实现、测试、菜单权限和前端联调，再扩展业务。

遇到问题先看具体模块的源码、Go 文档和本版本验证报告。网上其他版本的命令参数和模块路径不能直接复制；以 suxin COMMAND -h 和当前源码为准。
