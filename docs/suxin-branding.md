# GoSuxin 品牌交付

后台：http://127.0.0.1:8601/webadmin/

已更新品牌、公司、官网、多语言标题、项目自身作者说明、安装页、Logo、favicon、默认邮箱和 QQ；旧默认电话、ICP备案、公安备案及电信许可证展示已清空。项目 MIT 许可类型和第三方声明保留。

默认头像：`/resource/static/brand/logo.png?v=suxin1`。该目录单独开放静态访问；其他静态目录未扩大公开范围。当前管理员和旧默认头像已迁移，自定义头像保留，新用户未选择头像时自动填入品牌头像。

在线插件市场、购买和账号绑定显示筹备提示，远端代理请求在后端同步暂停。本地打包及插件安装入口保留，代码生成功能未修改。后续服务迁移须配置真实新站接口后再恢复，不能只开启开关。

前端源码和安装压缩包已同步，构建已部署。增量数据库脚本：`manifest/sql/suxin.sql`（默认 gf_ 前缀），可重复执行。当前数据库已执行，Ebook 未修改。

验证：Go 全量测试及编译、前端构建通过；类型检查加 --allowJs 后仍为原有 230 项，本次修改文件无诊断。真实浏览器验证登录、Logo、favicon、个人中心、默认头像及模拟切换存储域名、三种语言、深浅主题、筹备提示及本地打包表单通过，未产生原厂请求。API 验证管理员头像、静态内容、新用户默认头像、自定义头像保留及在线服务阻断通过；测试账号已删除。本次未实际安装第三方插件或生成业务代码包。

品牌资料中 Android 192/512 和 Apple 图标实际均为 108×108，保持原图使用，未放大。

私有备份：`runtime/pre-suxin/files.zip`（修改前源码、配置、安装包和部署资源）、`runtime/pre-suxin/database.sql`（相关数据表）。备份包含配置及账号数据，不应公开。截图：`runtime/suxin-login.png`、`runtime/suxin-profile.png`、`runtime/suxin-local-pack.png`。旧构建资源保留，新入口引用当前构建。

## 首页演示数据清理

首页仅保留当前登录账号的欢迎信息和数据库配置的快捷操作。移除写死的统计、模拟趋势与热门内容、分类占比、虚构最近访问和公告，以及演示轮播、无实际链接的文档入口。没有把未知统计改成 0，也没有删除业务数据。首页源码与安装包同步更新。
